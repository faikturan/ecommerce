version: "3"

services:
  
  mongo:
    image: mongo:4.4
    container_name: mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo-db:/data/db
      - mongo-configdb:/data/configdb

volumes:
  mongo-db:
  mongo-configdb:

